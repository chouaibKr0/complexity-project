[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "np-complexity-project"
version = "0.1.0"
description = "NP-Completeness: SAT, 3-SAT, Subset Sum"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v"
